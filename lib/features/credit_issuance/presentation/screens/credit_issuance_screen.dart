import 'package:flutter/material.dart';

class CreditIssuanceScreen extends StatelessWidget {
  const CreditIssuanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Solicitud de Crédito'),
      ),
      body: Center(
        child: Text('Solicitud de Crédito en construcción'),
      ),
    );
  }
}